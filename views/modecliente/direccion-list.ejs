<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    
</head>
<body>
    <%- include('../includes/header-cliente.ejs') %>
    <div class="container" style="margin-bottom: 130px;">
        <br>
        <h3>Mis direcciones</h3>
        <% if (success){ %>
            <div class="alert alert-success alert-dismissable">
                <button type="button" class="close" data-dismiss="alert">&times;</button>
                <strong>¡Éxito!</strong> Direccion <%=success%> correctamente
            </div>
        <%}%>

        <% if (error){ %>
            <div class="alert alert-danger alert-dismissable">
                <button type="button" class="close" data-dismiss="alert">&times;</button>
                <strong>¡Error!</strong> No se puede eliminar la Direccion
            </div>
        <%}%>
        
        
        <a href="/direccion-add" class="btn btn-success"><i class="fa fa-plus" aria-hidden="true"></i> Agregar dirección</a>
        <br>
        <br>
        <div>
            <table class="table">
                <thead>
                    <th>Dirección postal</th>
                    <th>Ciudad</th>
                    <th>Provincia</th>
                    <th>Código postal</th>
                    <th></th>
                </thead>
                <tbody>
                    <% data.forEach(item =>{ %>
                    <tr>
                        <td><%= item.direccion_postal %></td>
                        <td><%= item.ciudad %></td>
                        <td><%= item.provincia %></td>
                        <td><%= item.codigo_postal %></td>
                        <td class="text-right">
                            <a class="btn btn-danger" href="/direccion-delete?id=<%= item.id %>">Eliminar</a>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
    <%- include('../includes/footer-cliente.ejs') %>

    
</body>
</html>